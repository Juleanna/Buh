# 🖥️ Інструкція з використання BAT-скриптів

> **Облік ОЗ** — Повний набір скриптів для встановлення, налаштування, запуску та зупинки системи

---

## 📑 Зміст

- [🗺️ Загальна схема](#️-загальна-схема)
- [📋 Список усіх скриптів](#-список-усіх-скриптів)
- [🔧 install-prerequisites.bat](#-install-prerequisitesbat)
- [⚙️ setup.bat](#️-setupbat)
- [🗄️ create-db.bat](#️-create-dbbat)
- [🚀 start.bat](#-startbat)
- [🟢 start-backend.bat](#-start-backendbat)
- [🔵 start-frontend.bat](#-start-frontendbat)
- [🛑 stop.bat](#-stopbat)
- [❓ Часті питання (FAQ)](#-часті-питання-faq)
- [🔥 Вирішення проблем](#-вирішення-проблем)

---

## 🗺️ Загальна схема

Скрипти виконуються в певному порядку. Ось повний шлях від чистої системи до працюючого додатку:

```
┌─────────────────────────────────────────────────────────────────┐
│                    🖥️  ЧИСТА СИСТЕМА WINDOWS                    │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │  🔧 install-prerequisites   │  ← Крок 1 (один раз)
              │  Встановлює:                │
              │  • Python 3.12             │
              │  • Node.js LTS            │
              │  • PostgreSQL 16           │
              └─────────────┬───────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │  ⚙️  setup.bat               │  ← Крок 2 (один раз)
              │  Налаштовує:                │
              │  • .env конфігурація        │
              │  • Python venv + pip       │
              │  • База даних + міграції   │
              │  • Початкові дані          │
              │  • npm install             │
              └─────────────┬───────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │  🚀 start.bat               │  ← Крок 3 (щоразу)
              │  Запускає:                  │
              │  • Django backend           │
              │  • React frontend           │
              │  • Відкриває браузер        │
              └─────────────┬───────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │  ✅ Система працює!          │
              │  http://localhost:5173      │
              └─────────────┬───────────────┘
                            │
                            ▼
              ┌─────────────────────────────┐
              │  🛑 stop.bat                │  ← Коли потрібно зупинити
              │  Зупиняє всі сервіси       │
              └─────────────────────────────┘
```

### ⚡ Швидкий старт (3 команди)

```batch
:: 1. Встановити програми (потрібні права адміністратора)
install-prerequisites.bat

:: 2. Налаштувати проєкт
setup.bat

:: 3. Запустити систему
start.bat
```

---

## 📋 Список усіх скриптів

| Скрипт | Призначення | Коли запускати | Права адміна |
|--------|-------------|---------------|:------------:|
| 🔧 `install-prerequisites.bat` | Встановлення Python, Node.js, PostgreSQL | Один раз на новій системі | ✅ Потрібні |
| ⚙️ `setup.bat` | Повне налаштування проєкту з нуля | Один раз після клонування | ❌ Не потрібні |
| 🗄️ `create-db.bat` | Створення бази даних окремо | За потреби | ❌ Не потрібні |
| 🚀 `start.bat` | Запуск Backend + Frontend разом | Щоразу для роботи | ❌ Не потрібні |
| 🟢 `start-backend.bat` | Запуск тільки Django сервера | Для розробки | ❌ Не потрібні |
| 🔵 `start-frontend.bat` | Запуск тільки React dev-сервера | Для розробки | ❌ Не потрібні |
| 🛑 `stop.bat` | Зупинка всіх сервісів | Коли потрібно зупинити | ❌ Не потрібні |

---

## 🔧 install-prerequisites.bat

### 📝 Опис

Автоматично встановлює всі необхідні програми для роботи системи через **Windows Package Manager (winget)**. Цей скрипт потрібно запустити **лише один раз** на новому комп'ютері.

### 📦 Що встановлює

| Програма | Версія | Навіщо потрібна |
|----------|--------|-----------------|
| 🐍 **Python** | 3.12 | Мова програмування для backend (Django) |
| 🟩 **Node.js** | LTS (22.x) | Середовище для frontend (React + Vite) |
| 🐘 **PostgreSQL** | 16 | База даних |

### 🚀 Як запустити

1. **Натисніть правою кнопкою миші** на файл `install-prerequisites.bat`
2. Виберіть **«Запустити від імені адміністратора»**
3. Підтвердіть запит UAC (Windows запитає дозвіл)

> 💡 **Підказка:** Якщо запустити без прав адміністратора — скрипт автоматично перезапуститься з UAC-промптом.

### 📊 Що відбувається крок за кроком

```
╔════════════════════════════════════════════════╗
║   Облiк ОЗ — Встановлення програм              ║
║   Python, Node.js, PostgreSQL                   ║
╚════════════════════════════════════════════════╝

[OK] Права адмiнiстратора пiдтверджено.

[1/6] Перевiрка Windows Package Manager (winget)...
  Знайдено: winget v1.9.25200

[2/6] Перевiрка Python...
  Python не знайдено. Встановлення Python 3.12...     ← Автоматично!
  Python 3.12 встановлено.

[3/6] Перевiрка Node.js...
  Вже встановлено: Node.js v22.12.0                   ← Пропускає якщо є

[4/6] Перевiрка PostgreSQL...
  PostgreSQL не знайдено. Встановлення PostgreSQL 16...← Автоматично!
  PostgreSQL 16 встановлено.

[5/6] Оновлення PATH...

[6/6] Верифiкацiя встановлення...

  ┌──────────────┬───────────────────────────────┐
  │ Програма     │ Статус                        │
  │ Python       │ 3.12.8                        │
  │ Node.js      │ v22.12.0                      │
  │ PostgreSQL   │ 16.6                          │
  └──────────────┴───────────────────────────────┘

╔════════════════════════════════════════════════╗
║   Всi програми встановлено!                    ║
╚════════════════════════════════════════════════╝

  Запустити setup.bat зараз? (Y/N)
```

### ⚠️ Вимоги

- **Windows 10** версії 1709 або новіша / **Windows 11**
- **winget** (Windows Package Manager) — зазвичай вже встановлений
- **Інтернет-з'єднання** — для завантаження програм

### 🔧 Якщо winget не знайдено

1. Відкрийте **Microsoft Store**
2. Знайдіть **«App Installer»** (від Microsoft)
3. Натисніть **«Оновити»** або **«Встановити»**
4. Перезапустіть скрипт

Або завантажте з: https://aka.ms/getwinget

### ⏱️ Тривалість

- 🟢 Якщо все вже встановлено: **~5 секунд**
- 🟡 Якщо потрібно встановити 1-2 програми: **3–5 хвилин**
- 🔴 Якщо потрібно встановити все: **10–15 хвилин** (залежить від швидкості інтернету)

---

## ⚙️ setup.bat

### 📝 Опис

Повне налаштування проєкту з нуля. Створює віртуальне середовище Python, встановлює залежності, налаштовує базу даних, створює адміністратора та встановлює frontend-залежності.

### 🚀 Як запустити

Двічі клікніть на файл `setup.bat` (права адміністратора **не потрібні**).

### 📊 Що відбувається крок за кроком

```
╔════════════════════════════════════════════════╗
║   Облiк ОЗ — Повне встановлення з нуля        ║
╚════════════════════════════════════════════════╝

[1/8] Перевiрка Python...              ← Перевіряє наявність Python
  Знайдено: Python 3.12.8

[2/8] Перевiрка Node.js...             ← Перевіряє наявність Node.js
  Знайдено: Node.js v22.12.0

[3/8] Перевiрка PostgreSQL...          ← Перевіряє наявність PostgreSQL
  Знайдено: PostgreSQL 16.6

[4/8] Налаштування конфiгурацiї...     ← Створює .env файл
  Створено .env з шаблону .env.example
  *** ВАЖЛИВО: вiдредагуйте backend\.env ***
  (Відкриється Блокнот для редагування)

[5/8] Створення Python-середовища...   ← venv + pip + залежності
  Вiртуальне середовище створено.
  Оновлення pip...
  Встановлення Python-залежностей...
  Python-залежностi встановлено.

[6/8] Створення бази даних та мiграцiї...  ← БД + Django migrate
  База даних buh_assets створена.
  Мiграцiї виконано успiшно.

[7/8] Заповнення початкових даних...   ← 16 груп ОЗ + суперюзер
  16 груп ОЗ згiдно ПКУ ст. 138.3.3 створено.
  Створення суперкористувача:
  Username: admin                      ← Ви вводите логін
  Email: admin@example.com             ← Ви вводите email
  Password: ********                   ← Ви вводите пароль

[8/8] Встановлення frontend-залежностей...  ← npm install
  Frontend-залежностi встановлено.

╔════════════════════════════════════════════════╗
║   Встановлення завершено успiшно!              ║
╚════════════════════════════════════════════════╝
```

### 🔑 Крок 4 — Налаштування .env (детально)

На кроці 4 автоматично відкриється **Блокнот** з файлом `backend\.env`. Вам потрібно встановити **пароль PostgreSQL**:

```env
## Налаштування системи обліку основних засобів

# Django
DJANGO_SECRET_KEY=django-insecure-auto-generated-123456789
DEBUG=True

# PostgreSQL
POSTGRES_DB=buh_assets
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here    ← ⚠️ ЗМІНІТЬ НА СВІЙ ПАРОЛЬ!
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# CORS
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
```

> ⚠️ **Важливо:** Значення `POSTGRES_PASSWORD` повинно збігатися з паролем, який ви задали при встановленні PostgreSQL. Якщо ви використовували `install-prerequisites.bat`, це пароль з установника PostgreSQL.

### 🔑 Крок 7 — Створення суперкористувача (детально)

Скрипт запитає дані для адміністратора системи:

| Поле | Що вводити | Приклад |
|------|-----------|---------|
| 👤 **Username** | Логін адміністратора | `admin` |
| 📧 **Email** | Електронна пошта | `admin@company.com` |
| 🔒 **Password** | Пароль (не менше 8 символів) | `MySecurePass123` |
| 🔒 **Password (again)** | Повторіть пароль | `MySecurePass123` |

> 💡 **Підказка:** Ці дані використовуються для входу в систему за адресою http://localhost:5173 та в Django Admin за адресою http://localhost:8000/admin/

### ⏱️ Тривалість

- 🟡 Типовий час: **3–7 хвилин** (залежить від швидкості інтернету та диска)
- Найдовший крок — встановлення Python-залежностей (pip install)

### ⚠️ Можливі помилки

| Помилка | Причина | Рішення |
|---------|---------|---------|
| `Python не знайдено` | Python не встановлений або не в PATH | Запустіть `install-prerequisites.bat` |
| `Node.js не знайдено` | Node.js не встановлений | Запустіть `install-prerequisites.bat` |
| `Не вдалося створити вiртуальне середовище` | Пошкоджений Python | Перевстановіть Python |
| `Не вдалося встановити Python-залежностi` | Проблеми з мережею або pip | `cd backend & venv\Scripts\activate & pip install -r requirements.txt` |
| `Мiграцiї не вдалися` | PostgreSQL не запущений або невірний пароль | Перевірте службу PostgreSQL та `backend\.env` |
| `npm install не вдався` | Node.js не в PATH або проблеми з мережею | `cd frontend & npm install` |

---

## 🗄️ create-db.bat

### 📝 Опис

Створює базу даних `buh_assets` в PostgreSQL. Цей скрипт є **допоміжним** — `setup.bat` вже виконує цю дію автоматично. Використовуйте його, якщо потрібно створити базу окремо (наприклад, після перевстановлення PostgreSQL).

### 🚀 Як запустити

Двічі клікніть на файл `create-db.bat`.

### 📊 Що відбувається

```
╔════════════════════════════════════════════════╗
║   Облiк ОЗ — Створення бази даних             ║
╚════════════════════════════════════════════════╝

Читаю налаштування з backend\.env...       ← Автоматично бере з .env
  Користувач: postgres
  Хост: localhost:5432

Створення бази даних buh_assets...

[OK] База даних buh_assets створена успiшно!
```

### 🔧 Як це працює

1. 🔍 Перевіряє наявність `psql` в PATH
2. 📖 Читає налаштування підключення з `backend\.env` (користувач, хост, порт)
3. 🗄️ Виконує SQL-команду: `CREATE DATABASE buh_assets ENCODING 'UTF8';`
4. ✅ Повідомляє про результат

> 💡 **Підказка:** Якщо база вже існує — скрипт повідомить про це, але це не помилка.

### 🔒 Пароль

Під час виконання PostgreSQL запитає пароль користувача `postgres` — введіть той самий пароль, що і в `backend\.env`.

---

## 🚀 start.bat

### 📝 Опис

**Головний скрипт запуску.** Запускає одночасно Backend (Django) та Frontend (React), а потім автоматично відкриває браузер з додатком.

### 🚀 Як запустити

Двічі клікніть на файл `start.bat`.

### 📊 Що відбувається

```
╔════════════════════════════════════════════════╗
║   Облiк ОЗ — Запуск системи                   ║
╚════════════════════════════════════════════════╝

Запуск Backend (Django)...          ← Відкривається нове вікно (зелене)
Очiкування запуску backend...       ← Чекає 3 секунди
Запуск Frontend (React)...          ← Відкривається ще одне вікно (синє)

╔════════════════════════════════════════════════╗
║   Система запущена!                            ║
╚════════════════════════════════════════════════╝

  Backend:  http://localhost:8000
  Frontend: http://localhost:5173       ← Браузер відкриється автоматично!
  Адмiнка:  http://localhost:8000/admin/

  Для зупинки: stop.bat
```

### 🪟 Вікна які з'являться

Після запуску у вас буде **3 вікна командного рядка**:

| Вікно | Назва | Що робить | Можна закрити? |
|:-----:|-------|-----------|:--------------:|
| 1️⃣ | `Облiк ОЗ — Запуск системи` | Головне вікно | ✅ Так, після запуску |
| 2️⃣ | `Облiк ОЗ — Backend` | Django сервер (порт 8000) | ❌ Ні! Система зупиниться |
| 3️⃣ | `Облiк ОЗ — Frontend` | Vite dev-сервер (порт 5173) | ❌ Ні! Система зупиниться |

> ⚠️ **Важливо:** Вікна Backend та Frontend повинні залишатися відкритими, поки ви працюєте з системою! Щоб зупинити — використовуйте `stop.bat`.

### 🌐 Адреси після запуску

| Адреса | Що відкриється |
|--------|---------------|
| 🏠 http://localhost:5173 | **Головна сторінка** (React-додаток) |
| 🔧 http://localhost:8000/admin/ | **Django Admin** (адміністрування) |
| 📡 http://localhost:8000/api/ | **API** (для розробників) |

### ⚠️ Перевірки перед запуском

Скрипт автоматично перевіряє:
- ✅ Наявність `backend\venv` (Python-середовище встановлено)
- ✅ Наявність `frontend\node_modules` (Frontend-залежності встановлені)

Якщо чогось не вистачає — скрипт підкаже запустити `setup.bat`.

---

## 🟢 start-backend.bat

### 📝 Опис

Запускає **тільки Django-сервер** (backend). Корисно для розробки, коли потрібно працювати лише з API або Django Admin.

### 🚀 Як запустити

Двічі клікніть на файл `start-backend.bat`.

### 📊 Що відбувається

```
╔════════════════════════════════════════════════╗
║   Django Backend — http://localhost:8000       ║
╚════════════════════════════════════════════════╝

Застосування мiграцiй...              ← Автоматично оновлює БД
  Operations to perform:
    Apply all migrations: ...
  Running migrations:
    No migrations to apply.

Запуск Django сервера...
Натиснiть Ctrl+C для зупинки

Watching for file changes with StatReloader
Starting development server at http://0.0.0.0:8000/
```

### 🔧 Що робить скрипт

1. 📂 Переходить у папку `backend`
2. 🐍 Активує Python-середовище (`venv`)
3. 🗄️ Виконує міграції (`python manage.py migrate`)
4. 🚀 Запускає Django-сервер на порту **8000**

### ⏹️ Як зупинити

Натисніть **`Ctrl+C`** у вікні командного рядка, потім підтвердіть `Y`.

### 🌐 Що доступно

| Адреса | Опис |
|--------|------|
| http://localhost:8000/admin/ | Django Admin панель |
| http://localhost:8000/api/ | REST API |
| http://localhost:8000/api/docs/ | Swagger документація API |

---

## 🔵 start-frontend.bat

### 📝 Опис

Запускає **тільки React dev-сервер** (frontend). Корисно для розробки інтерфейсу, коли backend вже запущений окремо.

### 🚀 Як запустити

Двічі клікніть на файл `start-frontend.bat`.

### 📊 Що відбувається

```
╔════════════════════════════════════════════════╗
║   React Frontend — http://localhost:5173       ║
╚════════════════════════════════════════════════╝

Запуск Vite dev-сервера...
Натиснiть Ctrl+C для зупинки

  VITE v6.x.x  ready in 800 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.x.x:5173/
```

### 🔧 Що робить скрипт

1. 📂 Переходить у папку `frontend`
2. 📦 Перевіряє наявність `node_modules` (якщо немає — автоматично запускає `npm install`)
3. 🚀 Запускає Vite dev-сервер на порту **5173**

### ⏹️ Як зупинити

Натисніть **`Ctrl+C`** у вікні командного рядка.

### 🔄 Гаряче перезавантаження (HMR)

Vite автоматично оновлює сторінку в браузері при зміні файлів. Не потрібно перезапускати сервер після кожної зміни коду!

> ⚠️ **Важливо:** Frontend потребує працюючий backend для роботи з даними. Якщо backend не запущений, ви побачите помилки при спробі завантажити дані.

---

## 🛑 stop.bat

### 📝 Опис

Зупиняє **всі сервіси** системи — і Backend, і Frontend. Використовує два методи для гарантованої зупинки.

### 🚀 Як запустити

Двічі клікніть на файл `stop.bat`.

### 📊 Що відбувається

```
╔════════════════════════════════════════════════╗
║   Облiк ОЗ — Зупинка системи                  ║
╚════════════════════════════════════════════════╝

Зупинка Backend...                            ← Закриває вікна за назвою
Зупинка процесiв на порту 8000 (Django)...    ← Вбиває процеси на порту
Зупинка процесiв на порту 5173 (Vite)...      ← Вбиває процеси на порту

Усi сервiси зупинено.
```

### 🔧 Методи зупинки

Скрипт використовує **два методи** для надійної зупинки:

1. 🪟 **За назвою вікна** — закриває всі вікна з назвою `Oblik OZ*`
2. 🔌 **За портом** — знаходить і завершує процеси, які слухають порти 8000 та 5173

> 💡 Навіть якщо ви закрили вікна вручну, але процеси "зависли" — `stop.bat` їх знайде і завершить.

---

## ❓ Часті питання (FAQ)

### 🔹 Який порядок запуску на новому комп'ютері?

```
1️⃣  install-prerequisites.bat     (один раз — встановлює програми)
2️⃣  setup.bat                     (один раз — налаштовує проєкт)
3️⃣  start.bat                     (щоразу — запускає систему)
```

### 🔹 Я вже маю Python/Node.js/PostgreSQL, що запускати?

Пропустіть `install-prerequisites.bat` і одразу запустіть:
```
1️⃣  setup.bat     (налаштування проєкту)
2️⃣  start.bat     (запуск системи)
```

### 🔹 Як запустити систему наступного разу?

Просто запустіть `start.bat` — він зробить все необхідне. Повторно запускати `setup.bat` чи `install-prerequisites.bat` **не потрібно**.

### 🔹 Чи можна запускати Backend та Frontend окремо?

Так! Це зручно при розробці:

```batch
:: В одному терміналі:
start-backend.bat

:: В іншому терміналі:
start-frontend.bat
```

### 🔹 Як змінити порти?

| Сервіс | Файл | Де змінити |
|--------|------|-----------|
| Backend (8000) | `start-backend.bat` | Замініть `0.0.0.0:8000` на потрібний порт |
| Frontend (5173) | `frontend\vite.config.ts` | Додайте `server: { port: XXXX }` |

### 🔹 Як повністю перевстановити проєкт?

```batch
:: 1. Зупинити все
stop.bat

:: 2. Видалити артефакти (вручну)
rmdir /s /q backend\venv
rmdir /s /q frontend\node_modules
del backend\.env

:: 3. Налаштувати заново
setup.bat
```

### 🔹 Чи можна поставити систему на іншому ПК через локальну мережу?

Так! Після запуску `start.bat` система доступна з інших комп'ютерів у тій самій мережі:

- Дізнайтесь вашу IP-адресу: `ipconfig`
- Відкрийте на іншому ПК: `http://<ваш-IP>:5173`

> ⚠️ Переконайтесь, що Windows Firewall дозволяє вхідні з'єднання на порти 5173 та 8000.

---

## 🔥 Вирішення проблем

### 🐍 Python

<details>
<summary><strong>❌ «Python не знайдено»</strong></summary>

**Причина:** Python не встановлений або не доданий у PATH.

**Рішення:**
1. Запустіть `install-prerequisites.bat`
2. Або встановіть вручну з https://www.python.org/downloads/
3. ⚠️ При встановленні обов'язково поставте галочку **«Add Python to PATH»**
4. Після встановлення **перезапустіть** командний рядок

**Перевірка:**
```batch
python --version
:: Має вивести: Python 3.12.x
```
</details>

<details>
<summary><strong>❌ «Не вдалося створити вiртуальне середовище»</strong></summary>

**Причина:** Пошкоджена інсталяція Python або відсутній модуль `venv`.

**Рішення:**
1. Видаліть Python через «Програми та компоненти»
2. Перевстановіть Python з https://www.python.org/downloads/
3. При встановленні оберіть **«Customize installation»** та переконайтесь, що ✅ `pip` та ✅ `venv` відмічені
4. Запустіть `setup.bat` повторно
</details>

<details>
<summary><strong>❌ «Не вдалося встановити Python-залежностi»</strong></summary>

**Причина:** Проблеми з мережею, старий pip, або несумісні пакети.

**Рішення:**
```batch
cd backend
venv\Scripts\activate
python -m pip install --upgrade pip
pip install -r requirements.txt --verbose
```
Прочитайте повідомлення про помилку — зазвичай воно вказує на конкретний пакет.
</details>

---

### 🟩 Node.js

<details>
<summary><strong>❌ «Node.js не знайдено»</strong></summary>

**Причина:** Node.js не встановлений або не в PATH.

**Рішення:**
1. Запустіть `install-prerequisites.bat`
2. Або встановіть вручну з https://nodejs.org/ (оберіть LTS-версію)
3. Перезапустіть командний рядок

**Перевірка:**
```batch
node --version
npm --version
```
</details>

<details>
<summary><strong>❌ «npm install не вдався»</strong></summary>

**Причина:** Проблеми з мережею, кеш npm або несумісні залежності.

**Рішення:**
```batch
cd frontend
npm cache clean --force
npm install
```

Якщо не допомогло:
```batch
rmdir /s /q node_modules
del package-lock.json
npm install
```
</details>

---

### 🐘 PostgreSQL

<details>
<summary><strong>❌ «psql не знайдено в PATH»</strong></summary>

**Причина:** PostgreSQL встановлений, але шлях до `bin` не в PATH.

**Рішення:**
1. Знайдіть папку PostgreSQL (зазвичай `C:\Program Files\PostgreSQL\16\bin`)
2. Додайте її у змінну PATH:
   - 🪟 Win+R → `sysdm.cpl` → **Додатково** → **Змінні середовища**
   - У **Системних змінних** знайдіть `Path` → **Редагувати**
   - Додайте: `C:\Program Files\PostgreSQL\16\bin`
   - Натисніть **OK** → **OK**
3. Перезапустіть командний рядок

**Перевірка:**
```batch
psql --version
:: Має вивести: psql (PostgreSQL) 16.x
```
</details>

<details>
<summary><strong>❌ «Мiграцiї не вдалися»</strong></summary>

**Причина:** PostgreSQL не запущений, невірний пароль, або база даних не існує.

**Рішення:**

1. **Перевірте чи PostgreSQL запущений:**
   - 🪟 Win+R → `services.msc`
   - Знайдіть `postgresql-x64-16`
   - Стан повинен бути: **«Виконується»** / **«Running»**
   - Якщо зупинений → натисніть правою кнопкою → **«Запустити»**

2. **Перевірте пароль у `backend\.env`:**
   ```env
   POSTGRES_PASSWORD=ваш_реальний_пароль
   ```

3. **Перевірте чи база існує:**
   ```batch
   psql -U postgres -c "\l"
   :: Шукайте buh_assets у списку
   ```

4. **Створіть базу вручну:**
   ```batch
   create-db.bat
   :: або
   psql -U postgres -c "CREATE DATABASE buh_assets ENCODING 'UTF8';"
   ```
</details>

<details>
<summary><strong>❌ PostgreSQL запитує пароль, а я його не пам'ятаю</strong></summary>

**Рішення:**
1. Відкрийте файл `C:\Program Files\PostgreSQL\16\data\pg_hba.conf`
2. Знайдіть рядок:
   ```
   host  all  all  127.0.0.1/32  scram-sha-256
   ```
3. Змініть `scram-sha-256` на `trust`
4. Перезапустіть службу PostgreSQL (`services.msc`)
5. Тепер можна змінити пароль:
   ```batch
   psql -U postgres -c "ALTER USER postgres PASSWORD 'новий_пароль';"
   ```
6. Поверніть `trust` назад на `scram-sha-256`
7. Перезапустіть PostgreSQL
8. Оновіть пароль у `backend\.env`
</details>

---

### 🪟 Windows / Загальні

<details>
<summary><strong>❌ «winget не знайдено»</strong></summary>

**Причина:** Windows Package Manager (App Installer) не встановлений або застаріла версія Windows.

**Рішення:**
1. Відкрийте **Microsoft Store**
2. Знайдіть **«App Installer»** від Microsoft
3. Натисніть **«Оновити»** або **«Встановити»**
4. Перезапустіть `install-prerequisites.bat`

> ℹ️ winget доступний на Windows 10 (1709+) та Windows 11.
</details>

<details>
<summary><strong>❌ Програми встановились, але «не знайдено»</strong></summary>

**Причина:** PATH не оновився в поточному вікні.

**Рішення:**
1. Закрийте **всі** вікна командного рядка
2. Відкрийте **нове** вікно
3. Запустіть скрипт повторно

Або перезавантажте комп'ютер.
</details>

<details>
<summary><strong>❌ Скрипт показує кракозябри замість українських літер</strong></summary>

**Причина:** Неправильна кодова сторінка терміналу.

**Рішення:**
Усі скрипти вже містять `chcp 65001` для переключення на UTF-8. Якщо проблема залишається:
1. 🪟 Win+R → `intl.cpl`
2. Вкладка **«Адміністрування»** → **«Змінити мову системи»**
3. Поставте ✅ **«Beta: Use Unicode UTF-8 for worldwide language support»**
4. Перезавантажте комп'ютер
</details>

<details>
<summary><strong>❌ Порт 8000 або 5173 вже зайнятий</strong></summary>

**Причина:** Інший процес вже використовує порт.

**Рішення:**
```batch
:: 1. Зупиніть всі сервіси
stop.bat

:: 2. Перевірте що займає порт
netstat -aon | findstr :8000
netstat -aon | findstr :5173

:: 3. Якщо процес ще живий, завершіть його за PID
taskkill /f /pid <PID>
```
</details>

---

## 📊 Зведена таблиця

| Скрипт | Що робить | Коли | Час | Результат |
|--------|-----------|------|-----|-----------|
| 🔧 `install-prerequisites` | Python + Node.js + PostgreSQL | 1 раз | 5–15 хв | Програми встановлені |
| ⚙️ `setup` | venv + pip + DB + npm | 1 раз | 3–7 хв | Проєкт готовий |
| 🗄️ `create-db` | БД `buh_assets` | За потреби | 5 сек | БД створена |
| 🚀 `start` | Backend + Frontend | Щоразу | 10 сек | Система працює |
| 🟢 `start-backend` | Тільки Django | Розробка | 5 сек | API працює |
| 🔵 `start-frontend` | Тільки React | Розробка | 5 сек | UI працює |
| 🛑 `stop` | Зупинка всього | За потреби | 3 сек | Все зупинено |

---

<p align="center">
  <strong>🏛️ Облік основних засобів — Система готова до роботи!</strong><br>
  <em>Після запуску відкрийте: <a href="http://localhost:5173">http://localhost:5173</a></em>
</p>
