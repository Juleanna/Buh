# 🐳 Інструкція з розгортання «Облік ОЗ» у Docker

> **Система обліку основних засобів** — Django REST + React/Ant Design
> з підтримкою Celery, Redis, PostgreSQL та Google Drive бекапів.

---

## 📑 Зміст

1. [🔧 Вимоги](#-вимоги)
2. [📁 Структура проекту](#-структура-проекту)
3. [⚙️ Налаштування перед запуском](#️-налаштування-перед-запуском)
4. [🚀 Перший запуск](#-перший-запуск)
5. [🖥️ Доступ до системи](#️-доступ-до-системи)
6. [📋 Корисні команди Docker](#-корисні-команди-docker)
7. [🗄️ Робота з базою даних](#️-робота-з-базою-даних)
8. [💾 Резервне копіювання](#-резервне-копіювання)
9. [☁️ Google Drive бекап (опціонально)](#️-google-drive-бекап-опціонально)
10. [🔄 Оновлення проекту](#-оновлення-проекту)
11. [🛑 Зупинка та видалення](#-зупинка-та-видалення)
12. [🐛 Вирішення проблем](#-вирішення-проблем)
13. [🏗️ Архітектура контейнерів](#️-архітектура-контейнерів)

---

## 🔧 Вимоги

| Компонент | Мінімальна версія | Перевірка |
|-----------|-------------------|-----------|
| 🐳 Docker | 20.10+ | `docker --version` |
| 🐙 Docker Compose | 2.0+ (V2) | `docker compose version` |
| 💻 ОЗУ | 2 ГБ вільних | — |
| 💽 Диск | 3 ГБ вільних | — |
| 🌐 Порти | 80, 5432, 6379, 8000 | — |

### 📥 Встановлення Docker

<details>
<summary>🪟 Windows</summary>

1. Завантажте [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop/)
2. Запустіть інсталятор та дотримуйтесь інструкцій
3. Після встановлення перезавантажте комп'ютер
4. Запустіть Docker Desktop з меню «Пуск»
5. Дочекайтесь повідомлення **«Docker Desktop is running»**

> ⚠️ **Важливо:** Для Windows потрібен увімкнений **WSL 2** або **Hyper-V**.
> Docker Desktop запропонує встановити WSL 2 автоматично.

</details>

<details>
<summary>🐧 Linux (Ubuntu/Debian)</summary>

```bash
# Встановлення Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Додати поточного користувача до групи docker
sudo usermod -aG docker $USER

# Перезайти в сесію
newgrp docker

# Перевірка
docker --version
docker compose version
```

</details>

<details>
<summary>🍎 macOS</summary>

1. Завантажте [Docker Desktop для Mac](https://www.docker.com/products/docker-desktop/)
2. Перетягніть у папку Applications
3. Запустіть Docker Desktop
4. Дочекайтесь іконки 🐳 у меню-барі

</details>

---

## 📁 Структура проекту

```
📦 Buh/
├── 🐳 docker-compose.yml        ← Головний файл Docker оркестрації
├── 📄 DOCKER_GUIDE.md            ← Ця інструкція
│
├── 📂 backend/                    ← Django REST API
│   ├── 🐳 Dockerfile             ← Образ backend
│   ├── 🚪 entrypoint.sh          ← Скрипт ініціалізації
│   ├── 📄 requirements.txt       ← Python-залежності
│   ├── 🔒 .env.docker            ← Змінні оточення (Docker)
│   ├── 📄 .env.example           ← Шаблон змінних оточення
│   ├── 📂 config/                ← Налаштування Django
│   ├── 📂 apps/
│   │   ├── 📂 accounts/          ← 👤 Користувачі та авторизація
│   │   ├── 📂 assets/            ← 🏢 Основні засоби
│   │   ├── 📂 documents/         ← 📝 Документи та акти
│   │   └── 📂 reports/           ← 📊 Звіти та бекапи
│   └── 📂 templates/             ← HTML шаблони
│
├── 📂 frontend/                   ← React SPA
│   ├── 🐳 Dockerfile             ← Образ frontend (multi-stage)
│   ├── 🌐 nginx.conf             ← Конфігурація Nginx
│   ├── 📄 package.json           ← Node.js залежності
│   └── 📂 src/                   ← Вихідний код React
│       ├── 📂 pages/             ← Сторінки додатку
│       ├── 📂 components/        ← UI компоненти
│       └── 📂 api/               ← HTTP клієнт
```

---

## ⚙️ Налаштування перед запуском

### 1️⃣ Налаштування змінних оточення

Файл `backend/.env.docker` вже містить значення за замовчуванням для Docker.

Якщо потрібно змінити параметри — відредагуйте цей файл:

```bash
# Відкрити для редагування
notepad backend/.env.docker        # Windows
nano backend/.env.docker           # Linux/macOS
```

#### 📝 Основні змінні:

| Змінна | За замовчуванням | Опис |
|--------|-----------------|------|
| `DJANGO_SECRET_KEY` | `django-insecure-...` | 🔑 Секретний ключ Django. **Змініть у продакшні!** |
| `DEBUG` | `False` | 🐛 Режим налагодження. `False` для продакшн |
| `POSTGRES_DB` | `buh_assets` | 🗄️ Назва бази даних |
| `POSTGRES_USER` | `postgres` | 👤 Користувач БД |
| `POSTGRES_PASSWORD` | `postgres` | 🔒 Пароль БД. **Змініть у продакшні!** |

### 2️⃣ Налаштування суперюзера (опціонально)

За замовчуванням створюється суперюзер:
- 👤 **Логін:** `admin`
- 🔑 **Пароль:** `admin123`

Щоб змінити, відредагуйте `docker-compose.yml`:

```yaml
environment:
  DJANGO_SUPERUSER_USERNAME: ваш_логін
  DJANGO_SUPERUSER_PASSWORD: ваш_пароль
```

Або задайте через змінні оточення при запуску:

```bash
DJANGO_SUPERUSER_USERNAME=boss DJANGO_SUPERUSER_PASSWORD=secure123 docker compose up -d
```

### 3️⃣ Перевірка вільних портів

Переконайтесь, що порти не зайняті:

```bash
# Windows (PowerShell)
netstat -ano | findstr ":80 :5432 :6379 :8000"

# Linux/macOS
ss -tlnp | grep -E ':(80|5432|6379|8000)'
```

| Порт | Сервіс | Примітка |
|------|--------|----------|
| `80` | 🌐 Frontend (Nginx) | Основний вхід у систему |
| `8000` | ⚙️ Backend (Django) | API (проксюється через Nginx) |
| `5432` | 🗄️ PostgreSQL | База даних |
| `6379` | 📮 Redis | Брокер черг Celery |

> 💡 **Порада:** Якщо порт 80 зайнятий — змініть в `docker-compose.yml`:
> ```yaml
> ports:
>   - "3000:80"  # Тепер доступ через http://localhost:3000
> ```

---

## 🚀 Перший запуск

### Крок 1 — Збирання образів

```bash
cd Buh
docker compose build
```

> ⏳ Перша збірка займає **5-10 хвилин** (завантаження базових образів, встановлення залежностей).
> Наступні збірки будуть значно швидшими завдяки кешуванню Docker.

Що відбувається:
- 📦 `backend` — встановлюються Python-залежності, PostgreSQL клієнт
- 📦 `frontend` — встановлюються Node.js залежності, збирається React-додаток, копіюється в Nginx

### Крок 2 — Запуск всіх сервісів

```bash
docker compose up -d
```

Прапорець `-d` запускає контейнери у фоновому режимі (detached).

### Крок 3 — Перевірка статусу

```bash
docker compose ps
```

✅ Очікуваний результат — всі 6 сервісів у стані **running** або **Up**:

```
NAME              STATUS
buh_postgres      Up (healthy)
buh_redis         Up (healthy)
buh_backend       Up
buh_celery        Up
buh_celery_beat   Up
buh_frontend      Up
```

### Крок 4 — Перегляд логів запуску

```bash
# Всі сервіси
docker compose logs

# Тільки backend (тут видно міграції та створення суперюзера)
docker compose logs backend
```

✅ В логах backend ви маєте побачити:

```
=== Очікування на PostgreSQL ===
PostgreSQL доступний!
=== Застосування міграцій ===
Operations to perform:
  Apply all migrations: ...
=== Збирання статичних файлів ===
=== Створення суперюзера (якщо не існує) ===
Суперюзер створено
=== Запуск: gunicorn ... ===
```

---

## 🖥️ Доступ до системи

Після успішного запуску система доступна за адресами:

| Сервіс | URL | Опис |
|--------|-----|------|
| 🌐 **Веб-додаток** | [http://localhost](http://localhost) | Основний інтерфейс |
| ⚙️ **Django Admin** | [http://localhost/admin/](http://localhost/admin/) | Панель адміністратора Django |
| 📡 **API** | [http://localhost/api/](http://localhost/api/) | REST API (через Nginx проксі) |
| 📡 **API напряму** | [http://localhost:8000/api/](http://localhost:8000/api/) | REST API (прямий доступ) |

### 🔐 Вхід у систему

1. Відкрийте [http://localhost](http://localhost) у браузері
2. Введіть облікові дані:
   - 👤 **Логін:** `admin`
   - 🔑 **Пароль:** `admin123`
3. Натисніть «Увійти»

### 👥 Ролі користувачів

| Роль | Опис | Доступ |
|------|------|--------|
| 🛡️ **Адміністратор** (`admin`) | Повний доступ до системи | Все + бекапи + управління користувачами |
| 📊 **Бухгалтер** (`accountant`) | Робота з ОЗ та звітами | ОЗ, прихід, вибуття, амортизація, звіти |
| 📋 **Інвентаризатор** (`inventory_manager`) | Інвентаризація | Перегляд ОЗ, інвентаризація |

---

## 📋 Корисні команди Docker

### 🔄 Управління сервісами

```bash
# 🟢 Запустити всі сервіси
docker compose up -d

# 🔴 Зупинити всі сервіси
docker compose down

# 🔄 Перезапустити конкретний сервіс
docker compose restart backend
docker compose restart celery
docker compose restart frontend

# 📊 Статус сервісів
docker compose ps

# 📋 Логи в реальному часі (слідкування)
docker compose logs -f

# 📋 Логи конкретного сервісу (останні 100 рядків)
docker compose logs --tail=100 backend
docker compose logs --tail=100 celery
```

### 🔍 Інспекція контейнерів

```bash
# 🖥️ Відкрити термінал у контейнері backend
docker exec -it buh_backend bash

# 🐍 Відкрити Django shell
docker exec -it buh_backend python manage.py shell

# 🗄️ Підключитися до PostgreSQL
docker exec -it buh_postgres psql -U postgres -d buh_assets

# 📮 Підключитися до Redis
docker exec -it buh_redis redis-cli
```

### 📊 Моніторинг ресурсів

```bash
# 💻 Використання CPU/RAM/Мережі кожним контейнером
docker stats

# 💽 Розмір volumes (даних)
docker system df -v
```

---

## 🗄️ Робота з базою даних

### 📤 Створення дампу бази (вручну через CLI)

```bash
# SQL дамп
docker exec buh_postgres pg_dump -U postgres buh_assets > backup.sql

# Стиснений дамп
docker exec buh_postgres pg_dump -U postgres buh_assets | gzip > backup.sql.gz
```

### 📥 Відновлення бази з дампу

```bash
# ⚠️ Це перезапише поточні дані!
# Спершу видалимо та створимо базу заново:
docker exec buh_postgres psql -U postgres -c "DROP DATABASE IF EXISTS buh_assets;"
docker exec buh_postgres psql -U postgres -c "CREATE DATABASE buh_assets;"

# Завантажимо дамп:
docker exec -i buh_postgres psql -U postgres -d buh_assets < backup.sql

# Перезапустити backend для застосування міграцій (якщо потрібно):
docker compose restart backend
```

### 🔄 Застосування нових міграцій

```bash
docker exec buh_backend python manage.py migrate
```

### 👤 Створення додаткового суперюзера

```bash
docker exec -it buh_backend python manage.py createsuperuser
```

---

## 💾 Резервне копіювання

### 🌐 Через веб-інтерфейс (рекомендовано)

1. Увійдіть як **адміністратор**
2. Перейдіть у меню **«💾 Резервна копія»**
3. Оберіть формат:
   - 📄 **SQL** — повний дамп PostgreSQL (рекомендовано для відновлення)
   - 📋 **JSON** — дамп через Django dumpdata (портативний формат)
4. Натисніть **«Завантажити»** — файл збережеться на ваш комп'ютер

### 🖥️ Через командний рядок

```bash
# 📄 SQL бекап з позначкою часу
docker exec buh_postgres pg_dump -U postgres buh_assets > "backup_$(date +%Y%m%d_%H%M%S).sql"

# 📋 JSON бекап через Django
docker exec buh_backend python manage.py dumpdata \
  --natural-foreign --natural-primary --indent 2 \
  --exclude contenttypes --exclude auth.Permission \
  --exclude sessions --exclude admin.logentry \
  > "backup_$(date +%Y%m%d_%H%M%S).json"
```

### 🔄 Відновлення з резервної копії

#### Відновлення з SQL-бекапу

> ⚠️ **Увага:** Ця операція **повністю замінить** всі поточні дані в базі! Спершу зробіть бекап поточного стану.

```bash
# 1. Зупинити backend (щоб не було активних з'єднань)
docker compose stop backend celery celery-beat

# 2. Видалити поточну базу та створити порожню
docker exec buh_postgres psql -U postgres -c "DROP DATABASE IF EXISTS buh_assets;"
docker exec buh_postgres psql -U postgres -c "CREATE DATABASE buh_assets;"

# 3. Завантажити дамп з файлу
docker exec -i buh_postgres psql -U postgres -d buh_assets < backup_buh_assets_20260221.sql

# 4. Запустити backend (міграції застосуються автоматично)
docker compose start backend celery celery-beat
```

#### Відновлення з JSON-бекапу

```bash
# 1. Зупинити backend
docker compose stop backend celery celery-beat

# 2. Скинути БД та застосувати міграції (створить порожні таблиці)
docker exec buh_backend python manage.py flush --noinput
docker exec buh_backend python manage.py migrate --noinput

# 3. Скопіювати файл бекапу в контейнер
docker cp backup_buh_assets_20260221.json buh_backend:/app/backup.json

# 4. Завантажити дані
docker exec buh_backend python manage.py loaddata /app/backup.json

# 5. Видалити тимчасовий файл та запустити сервіси
docker exec buh_backend rm /app/backup.json
docker compose start backend celery celery-beat
```

#### Відновлення з хмарного бекапу (Google Drive)

Хмарний бекап — це ZIP-архів, що містить `database.sql`, папку `media/` та файл `env_backup`.

```bash
# 1. Завантажте ZIP з Google Drive (через веб-інтерфейс або вручну)

# 2. Розпакуйте архів
unzip backup_20260221_020000.zip -d restore_temp/

# 3. Відновіть базу з SQL-дампу
docker compose stop backend celery celery-beat
docker exec buh_postgres psql -U postgres -c "DROP DATABASE IF EXISTS buh_assets;"
docker exec buh_postgres psql -U postgres -c "CREATE DATABASE buh_assets;"
docker exec -i buh_postgres psql -U postgres -d buh_assets < restore_temp/database.sql

# 4. Відновіть медіа-файли (якщо потрібно)
docker cp restore_temp/media/. buh_backend:/app/media/

# 5. Запустити сервіси
docker compose start backend celery celery-beat

# 6. Прибрати тимчасові файли
rm -rf restore_temp/
```

> 💡 **Порада:** Після відновлення завжди перевіряйте, чи працює система — увійдіть у веб-інтерфейс та перевірте наявність даних.

### ⏰ Автоматичне регулярне копіювання

Через веб-інтерфейс (потрібна інтеграція з Google Drive):

1. **«💾 Резервна копія»** → секція Google Drive
2. Увімкніть **автобекап**
3. Задайте час (наприклад, щоденно о 02:00)

Celery Beat автоматично створюватиме бекапи за розкладом.

---

## ☁️ Google Drive бекап (опціонально)

### Крок 1 — Створення OAuth2 credentials у Google Cloud

1. Перейдіть на [Google Cloud Console](https://console.cloud.google.com/)
2. Створіть новий проект або оберіть існуючий
3. Увімкніть **Google Drive API**:
   - Меню → APIs & Services → Library
   - Знайдіть «Google Drive API» → Enable
4. Створіть OAuth2 credentials:
   - APIs & Services → Credentials → Create Credentials → OAuth Client ID
   - Application type: **Web application**
   - Authorized redirect URIs: `http://localhost/api/reports/backup/gdrive-callback/`
   - Завантажте JSON файл

### Крок 2 — Налаштування проекту

1. Покладіть завантажений JSON файл у папку `backend/` з назвою `gdrive_credentials.json`

2. Відредагуйте `backend/.env.docker`:
   ```env
   GDRIVE_CREDENTIALS_PATH=gdrive_credentials.json
   GDRIVE_FOLDER_ID=ваш_id_папки_на_google_drive
   ```

3. Перезапустіть backend:
   ```bash
   docker compose restart backend
   ```

### Крок 3 — Авторизація

1. Відкрийте веб-інтерфейс → **«💾 Резервна копія»**
2. У секції Google Drive натисніть **«Підключити»**
3. Пройдіть OAuth авторизацію через Google
4. Після успішного підключення з'явиться статус ✅

### Крок 4 — Отримання ID папки Google Drive

1. Відкрийте [Google Drive](https://drive.google.com/)
2. Створіть папку для бекапів (наприклад, «Бекапи ОЗ»)
3. Відкрийте цю папку
4. Скопіюйте ID з URL:
   ```
   https://drive.google.com/drive/folders/ЦЕЙІДСКОПІЮЙТЕ
   ```

---

## 🔄 Оновлення проекту

### 📦 Після змін у коді

```bash
# 1. Зупинити сервіси
docker compose down

# 2. Перезібрати образи
docker compose build

# 3. Запустити з новими образами
docker compose up -d
```

### ⚡ Швидке оновлення (без зупинки)

```bash
# Перезібрати тільки backend
docker compose build backend
docker compose up -d backend

# Перезібрати тільки frontend
docker compose build frontend
docker compose up -d frontend
```

### 🧹 Повне очищення та перезбірка

```bash
# Зупинити все
docker compose down

# Перезібрати без кешу
docker compose build --no-cache

# Запустити
docker compose up -d
```

### 📋 Після додавання нових Python-залежностей

Якщо змінився `requirements.txt`:

```bash
docker compose build backend
docker compose up -d backend celery celery-beat
```

### 📋 Після додавання нових Node.js залежностей

Якщо змінився `package.json`:

```bash
docker compose build frontend
docker compose up -d frontend
```

---

## 🛑 Зупинка та видалення

### ⏸️ Зупинка (дані зберігаються)

```bash
docker compose down
```

### 🗑️ Повне видалення (⚠️ дані будуть втрачені!)

```bash
# Зупинити та видалити контейнери + volumes
docker compose down -v

# Видалити створені образи
docker compose down --rmi all -v
```

### 🧹 Очищення системи Docker

```bash
# Видалити невикористані образи, контейнери, мережі
docker system prune -a

# Видалити невикористані volumes (⚠️ обережно!)
docker volume prune
```

---

## 🐛 Вирішення проблем

### ❌ Контейнер не запускається

```bash
# Перевірити логи проблемного сервісу
docker compose logs backend
docker compose logs frontend

# Перевірити статус
docker compose ps -a
```

### ❌ Помилка «port is already in use»

Порт вже зайнятий іншою програмою.

```bash
# Windows — знайти процес на порту 80
netstat -ano | findstr :80
# Завершити процес за PID
taskkill /PID <номер_процесу> /F

# Linux — знайти та завершити
sudo lsof -i :80
sudo kill <PID>
```

Або змініть порт у `docker-compose.yml`:
```yaml
frontend:
  ports:
    - "3000:80"    # Замість 80 використати 3000
```

### ❌ Backend не підключається до БД

```bash
# Перевірити, чи працює PostgreSQL
docker compose ps db
docker compose logs db

# Перезапустити
docker compose restart db
# Зачекати 10 секунд
docker compose restart backend
```

### ❌ Помилка міграцій

```bash
# Подивитись деталі помилки
docker compose logs backend | grep -A 5 "migrate"

# Запустити міграції вручну
docker exec buh_backend python manage.py migrate --noinput

# Якщо міграція конфліктує — скинути та створити заново (⚠️ втрата даних!)
docker exec buh_backend python manage.py migrate apps_name zero
docker exec buh_backend python manage.py migrate
```

### ❌ Frontend показує білий екран

```bash
# Перевірити, чи зібрався frontend
docker compose logs frontend

# Перезібрати frontend
docker compose build frontend
docker compose up -d frontend
```

### ❌ API повертає 502 Bad Gateway

Backend ще не запустився або впав.

```bash
# Перевірити стан backend
docker compose ps backend
docker compose logs --tail=50 backend

# Перезапустити
docker compose restart backend
```

### ❌ Celery задачі не виконуються

```bash
# Перевірити стан Celery
docker compose ps celery
docker compose logs --tail=50 celery

# Перевірити Redis
docker compose exec redis redis-cli ping
# Очікувана відповідь: PONG

# Перезапустити Celery
docker compose restart celery celery-beat
```

### ❌ Бекап SQL не працює (pg_dump error)

```bash
# Перевірити, чи доступний pg_dump в контейнері
docker exec buh_backend which pg_dump
docker exec buh_backend pg_dump --version

# Перевірити підключення до БД
docker exec buh_backend python -c "
import psycopg
conn = psycopg.connect('host=db dbname=buh_assets user=postgres password=postgres')
print('OK')
conn.close()
"
```

### 🔑 Забули пароль адміністратора

```bash
docker exec -it buh_backend python manage.py changepassword admin
```

---

## 🏗️ Архітектура контейнерів

```
┌─────────────────────────────────────────────────────────┐
│                    Docker Network                       │
│                                                         │
│  ┌──────────┐    ┌──────────────┐    ┌───────────────┐  │
│  │ 🌐       │    │ ⚙️           │    │ 🗄️           │  │
│  │ Frontend │───▶│   Backend    │───▶│  PostgreSQL   │  │
│  │ (Nginx)  │    │  (Gunicorn)  │    │   (port 5432) │  │
│  │ port 80  │    │  port 8000   │    │               │  │
│  └──────────┘    └──────┬───────┘    └───────────────┘  │
│                         │                               │
│                         │            ┌───────────────┐  │
│                         │            │ 📮            │  │
│                         └───────────▶│    Redis      │  │
│                         │            │  (port 6379)  │  │
│  ┌──────────┐    ┌──────┴───────┐    └───────────────┘  │
│  │ ⏰       │    │ 🔄           │                       │
│  │ Celery   │    │   Celery     │                       │
│  │  Beat    │───▶│   Worker     │                       │
│  │(планувал)│    │ (виконавець) │                       │
│  └──────────┘    └──────────────┘                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 📦 Описи контейнерів

| Контейнер | Образ | Призначення |
|-----------|-------|-------------|
| `buh_frontend` | Node 20 → Nginx Alpine | 🌐 SPA React + проксі до API |
| `buh_backend` | Python 3.12-slim | ⚙️ Django REST API + Gunicorn |
| `buh_postgres` | PostgreSQL 16 Alpine | 🗄️ Основна база даних |
| `buh_redis` | Redis 7 Alpine | 📮 Брокер повідомлень для Celery |
| `buh_celery` | Python 3.12-slim | 🔄 Воркер для асинхронних задач |
| `buh_celery_beat` | Python 3.12-slim | ⏰ Планувальник періодичних задач |

### 💽 Docker Volumes (постійні дані)

| Volume | Призначення | Що зберігає |
|--------|-------------|-------------|
| `postgres_data` | 🗄️ Дані PostgreSQL | Вся база даних |
| `backend_media` | 🖼️ Медіа-файли | Завантажені документи, фото |
| `backend_static` | 📁 Статичні файли | CSS/JS Django Admin |
| `backend_backups` | 💾 Бекапи | Тимчасові файли резервних копій |

> 💡 **Важливо:** Доки ви не виконаєте `docker compose down -v`, всі дані (база, файли) зберігаються
> навіть після зупинки контейнерів.

---

## 📞 Швидка довідка

```bash
# 🟢 Запустити
docker compose up -d

# 🔴 Зупинити
docker compose down

# 📊 Статус
docker compose ps

# 📋 Логи
docker compose logs -f

# 🔄 Перезібрати та запустити
docker compose build && docker compose up -d

# 🖥️ Термінал backend
docker exec -it buh_backend bash

# 🗄️ Термінал БД
docker exec -it buh_postgres psql -U postgres -d buh_assets

# 💾 Бекап БД
docker exec buh_postgres pg_dump -U postgres buh_assets > backup.sql
```

---

> 📅 Останнє оновлення: Лютий 2026
